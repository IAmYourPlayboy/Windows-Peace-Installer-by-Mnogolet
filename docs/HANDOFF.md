# Передача: состояние проекта на 11.08.2026

> Прочитай этот файл целиком до того, как что-либо делать.
> Дальше по порядку: [ROADMAP.md](ROADMAP.md) → [ARCHITECTURE.md](ARCHITECTURE.md) → [приёмка шага А](superpowers/notes/2026-08-11-step-a-acceptance.md).

## Коротко

**Шаг А сделан целиком.** Появился первый код проекта: одиннадцать задач плана выполнены, каждая со своим коммитом. Приложение запускается на обычной Windows, показывает настоящие диски и разделы, даёт выбрать цель и показывает предпросмотр разметки. Ни одной операции записи на диск — это осознанно.

Тестов 87, каждый из ядра выполняется дважды — под `net48` и под `net8.0-windows`. Полная пересборка проходит без единого предупреждения.

**Твой следующий шаг — шаг Б: мастер в WinPE.** Спеки под него ещё нет; начинать надо с неё, а не с кода.

## Что за проект

Windows Peace — инструмент развёртывания настроенной Windows 11. Раньше назывался Razvert, переименован 10.08.2026.

Три части, так их видит автор:

1. **`WindowsPeace.Studio`** — программа на обычном компьютере. Скачивает Windows, делает из флешки установочный носитель, кладёт на неё один или несколько рецептов.
2. **`WindowsPeace.Setup` и `WindowsPeace.Agent`** — мастер установки с глубокой настройкой и доводка системы после. Здесь ядро ценности и весь технический риск, поэтому делается первой.
3. **Мини-сайт документации** — в самом конце.

Проект меценатский: планируется сайт, где незнакомым людям бесплатно предлагают установить настроенную систему.

## Что произошло в этой сессии

### Написан весь шаг А

Порядок был такой: каркас решения → отпечаток диска → классификация разделов → журнал и предельные времена → модель дисков → правила выбора → предпросмотр разметки → определение содержимого → перечисление через WMI → оболочка мастера → экран выбора диска.

Вся логика лежит в `WindowsPeace.Core` и не знает про интерфейс. Проверяется механически: в проекте нет ссылок на `PresentationFramework`. Окно — тонкая надстройка, которую на шаге Б можно переписать, не тронув ядро.

### Главный вопрос спеки закрыт: серийные номера читаются

На всех трёх дисках машины автора — SATA HDD, NVMe SSD, USB-флешка — серийный номер взят из первого же источника цепочки, `MSFT_PhysicalDisk.SerialNumber`. Уровень доверия вышел `Hardware`, то есть режим `pinned` из рецепта допустим. Откатываться на GUID разметки не пришлось ни разу.

Вся архитектура стоит на том, что диск адресуется отпечатком, а не порядковым номером. **На настоящем железе приём работает.** Подробности — в [слепке железа](superpowers/notes/2026-08-11-disk-dump.md).

### Найдены и исправлены три дефекта

Все три нашлись не в тестах, а при запуске на живой машине. Это довод в пользу того, что проверка на настоящем железе в конце каждого шага обязательна.

**Windows не находилась вовсе.** Спека предписывала искать куст реестра `\Windows\System32\config\SYSTEM`. Он закрыт правами доступа: без прав администратора проверка отвечает «файла нет» даже там, где Windows заведомо стоит. На диске D: автора стоит вторая Windows — предупреждение «всё будет стёрто безвозвратно» не появилось бы. Добавлен второй признак: файл ядра `ntoskrnl.exe`, читаемый кем угодно. Спека обновлена.

**Экран открывался с уже выбранным диском.** Список WPF по умолчанию синхронизирует выделение с текущим элементом коллекции, а текущий — первый. Кнопка «Далее» была активна, хотя человек ничего не выбирал. Для инструмента, который форматирует диски, недопустимо. Лечится `IsSynchronizedWithCurrentItem="False"`.

**Строки списка называли себя именем класса.** Средствам доступности каждая строка представлялась как `WindowsPeace.Setup.Pages.DiskRowViewModel`. Добавлен `ToString`.

### Расхождения с текстом плана

План был написан подробно, но в нём нашлись места, которые просто не компилируются или не работают. Все правки объяснены в сообщениях соответствующих коммитов; здесь — список, чтобы не искать.

| Что | Почему |
|---|---|
| `NuGet.config` в корне репозитория | В глобальной настройке машины список источников пуст, восстановление не находило ни одного пакета. Теперь сборка не зависит от настроек компьютера |
| `App.xaml` заведён в задаче 1, а не в 10 | Проект объявлен оконным приложением; без точки входа решение не собиралось вовсе |
| `.gitattributes` | Перевод строки CRLF, как объявлено в `.editorconfig` |
| Фабрики `SelectionTarget` получили приставку `For` | Метод `Partition` и свойство `Partition` в одном типе — ошибка компиляции CS0102 |
| `Assert.Single` с фильтром, `Assert.Equal` с константой первым аргументом | Требования анализатора xUnit, а предупреждения у нас приравнены к ошибкам |
| `using System.Collections.Generic` в `ViewModelBase` | Без него `EqualityComparer` не находится |
| Последний рубеж перехвата в `WmiDiskEnumerator.Enumerate` | Перечисления конкретных типов исключений мало для обещанного спекой «не падает». Единственное место в проекте, где ловится любое исключение; текст уходит в журнал и на экран |
| Тесты на `JsonLinesOperationLog` | План оставил файловый журнал без проверки, хотя сам требует писать тесты до реализации |

## Открытый вопрос к автору

**Перечисление дисков идёт в потоке интерфейса.** Замерено 240–1152 мс, и на это время окно не отвечает. Раздел 9 архитектуры требует другого: «любая работа дольше сотни миллисекунд уходит в фон, показывает продвижение и допускает отмену».

Это расхождение между планом и архитектурой, а не дефект реализации: план сознательно сделал модель экрана синхронной, чтобы её было проще проверять тестами. Работа небольшая и локальная — затрагивает `DiskPickerViewModel` и разметку экрана. **Решение за автором:** делать сейчас или на шаге Б, когда станет понятно, как ведёт себя WinPE.

## Решения автора — не переспрашивай

| Вопрос | Ответ |
|---|---|
| Версия Windows | Windows 11 Pro 25H2, значит `isoInjection` для Atlas доступен |
| Режим загрузки | Только UEFI + GPT |
| Порядок работ | Сначала окно, потом WinPE, потом запись на диск. Стенд Hyper-V строится к шагу В, не раньше |
| Разделы в интерфейсе | Список двухуровневый, кнопки «Создать», «Удалить», «Форматировать», «Расширить» **остаются**. Довод автора: «я не имею права говорить пользователям, что им это не нужно» |
| Раскладка экрана | По смыслу как в установщике Windows. Визуал автор настраивает сам |
| Несколько Windows | Имеется в виду **несколько рецептов на одной флешке**, а не мультизагрузка |
| Среда выполнения | Самодостаточное .NET 8. Допущение до опыта на шаге Б |
| Целевые платформы | `Core` — `net48;net8.0-windows`, `Setup` — только `net8.0-windows` |
| Ветки и отправка | Работа в отдельной ветке, слияние в `main` в конце шага, одна отправка на GitHub в конце |

## Чего ждать от машины

- **Прав администратора нет.** Проверено: `админ в токене: False`. На шаге А это уже сказалось — см. дефект с кустом реестра. DISM для монтирования `boot.wim` откажет. Понадобится на шаге Б: либо автор запустит Claude Code от администратора, либо выполнит команды сам.
- `dotnet` 8.0.423, Visual Studio нет. Всё через CLI.
- Hyper-V установлен, служба `vmms` работает, прав на неё нет.
- 7-Zip доступен как `7z` — им читается содержимое WIM без прав.
- **Источники NuGet в настройках машины пусты.** Лечится файлом `NuGet.config` в корне репозитория, он уже там. Не удаляй его.
- Диски машины: SATA HDD 931,5 ГБ (D:, там вторая Windows), NVMe SSD 476,9 ГБ (C:, работающая система), USB 58,6 ГБ (E: — установочная флешка Windows, записанная Rufus).
- Git настроен, SSH к GitHub работает, `origin` — `git@github.com:IAmYourPlayboy/Windows-Peace-Installer-by-Mnogolet.git`.

### Как проверять окно без рук

Пригодится на шаге Б и дальше. Приложение не установлено в системе, поэтому общие средства управления экраном его не видят. Работает другое:

- снимок окна — `PrintWindow` с флагом `PW_RENDERFULLCONTENT`, снимает только своё окно;
- управление — UI Automation из PowerShell (`UIAutomationClient`). Строки списка с колонками отдаются типом `DataItem`, а не `ListItem`;
- нажатие кнопки, открывающей модальное окно, блокирует поток — нажимать надо из отдельного процесса;
- запущенное через `Start-Process` окно приходит свёрнутым, разворачивать `ShowWindow(h, 4)`.

## Открытые вопросы

Не выдавай их за решённые.

1. **Заработает ли самодостаточное .NET 8 в WinPE.** Шаг Б. Запасные пути: `WinPE-NetFx` из ADK, затем HTA.
2. **Читаются ли серийные номера у виртуальных дисков Hyper-V.** На настоящем железе читаются, на виртуальном — неизвестно. Шаг В.
3. **Переживает ли NVRAM гостя чекпоинт и экспорт Hyper-V.** От этого зависит устройство грязного сценария на шаге В.
4. **Дефект B ещё не воспроизведён на стенде.** Механизм прочитан в коде рабочего `autounattend.xml` автора, но не подтверждён опытом. Критерий готовности шага В.

## Как работать над этим проектом

Прямые указания автора. Соблюдай их — за нарушение первого он уже делал замечание.

- **Объясняй приземлённо.** Не «NVRAM хранит BootOrder», а «материнская плата помнит, откуда грузиться». Термин вводи один раз и сразу переводи. При выборе из вариантов пиши конкретно: что делаю я, что делаешь ты, сколько времени, что получишь, что при этом плохо.
- **Качество и логика важнее краткости.** Думать столько, сколько нужно; скорость не важна. Но изложение — человеческое.
- **Никакой кустарщины.** Только систематические решения, рассчитанные на долгую жизнь.
- **Дробить на подзадачи.** Понемногу, постепенно.
- **Сначала видимое, потом оснастка.** Проверяй порядок работ вопросом «что автор увидит в конце этого шага».
- **Не отбирать у пользователя возможности** со словами «это вам не нужно».
- **Спрашивать, если ответ меняет архитектуру.** Не угадывать.
- **Проверять на живом железе.** Все три дефекта шага А нашлись при запуске, а не в тестах.

## Состояние репозитория

```
WindowsPeace/
├─ README.md
├─ .gitignore, .gitattributes, .editorconfig
├─ NuGet.config                                источник пакетов, не удалять
├─ Directory.Build.props                       предупреждения = ошибки
├─ WindowsPeace.sln
├─ contract/
│   ├─ recipe.schema.json                      единый контракт
│   └─ examples/atlas-25h2-ru.recipe.json
├─ src/
│   ├─ WindowsPeace.Core/                      вся логика, без интерфейса
│   │   ├─ Diagnostics/                        журнал, предельные времена
│   │   ├─ Selection/                          правила выбора, предпросмотр разметки
│   │   ├─ Storage/                            диски, разделы, отпечаток, WMI
│   │   └─ Polyfills/
│   └─ WindowsPeace.Setup/                     окно WPF
│       ├─ Infrastructure/, Shell/, Pages/
├─ test/
│   ├─ WindowsPeace.Core.Tests/                71 теста × две цели
│   └─ WindowsPeace.Setup.Tests/               16 тестов
├─ tools/DiskDump/                             печатает то, что видит перечислитель
└─ docs/
    ├─ ARCHITECTURE.md, ROADMAP.md, HANDOFF.md
    └─ superpowers/
        ├─ specs/2026-08-10-disk-picker-design.md
        ├─ plans/2026-08-10-step-a-disk-picker.md
        └─ notes/
            ├─ 2026-08-11-disk-dump.md          первый слепок железа
            ├─ 2026-08-11-step-a-acceptance.md  приёмка шага А
            └─ 2026-08-11-step-a-screen.png     как выглядит экран
```

Команды, которые понадобятся сразу:

```
dotnet test                         прогнать всё
dotnet run --project tools/DiskDump печатает диски машины
dotnet run --project src/WindowsPeace.Setup   открывает окно
```
